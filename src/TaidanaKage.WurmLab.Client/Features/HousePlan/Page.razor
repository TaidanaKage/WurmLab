@page "/HousePlan"

<PageTitle>WurmLab - House Plan</PageTitle>

<MudBreadcrumbs Items="_breadcrumbs" />

<MudText Typo="Typo.h2" Class="pb-5">House Plan</MudText>

<GroundFloorPlan />

<MudText Class="mt-5">Notes</MudText>

<MudList T="string" Dense="true">
    <MudListItem T="string">The maximum length of a house is 20 tiles in one direction (engine hard cap).</MudListItem>
</MudList>

@code {
    private readonly List<BreadcrumbItem> _breadcrumbs =
    [
        new BreadcrumbItem("Home", href: "./"),
        new BreadcrumbItem("House Plan", href: null, disabled: true)
    ];
}
